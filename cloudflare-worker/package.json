{
  "name": "cmms-worker",
  "version": "3.01",
  "private": true,
  "description": "CSC CMMS backend — Cloudflare Worker (1:1 with the deployed code)",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
